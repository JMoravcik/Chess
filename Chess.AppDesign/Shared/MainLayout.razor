@inherits LayoutComponentBase
@if (UserService.IsLoggedIn != null)
{
    <page>
        @if (UserService.IsLoggedIn.Value)
        {
            @Body
        }
        else
        {
            <Login/>
        }
    </page>
}
